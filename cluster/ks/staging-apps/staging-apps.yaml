apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: staging-apps
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./cluster/apps/manifests
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster-staging
  ignore: |
    # exclude all
    /*
